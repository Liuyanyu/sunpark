const app = state => state.app
const device = state => state.app.device
const menu = state => state.app.menu
const isEnglish = state => state.app.isEnglish

export {
    app,
    device,
    menu,
    isEnglish
}
